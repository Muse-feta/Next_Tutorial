const ProductDetailsFeatures = ({
  children,
}: {
  children: React.ReactNode;
}) => {
  return (
    <div>
      {children}
      <h1>Muse</h1>
    </div>
  );
};

export default ProductDetailsFeatures;
